---
layout: page.njk
section: dev
metaLabel: tools · experiments
title: Dev
deck: Things I build to make the terminal a nicer place to live.
---
<ul class="index">
  {% for project in dev %}
  <li>
    <p class="meta">{{ project.meta }}</p>
    <h2><a href="{{ project.url }}">{{ project.title }}</a></h2>
    <p>{{ project.blurb }}</p>
  </li>
  {% endfor %}
</ul>
