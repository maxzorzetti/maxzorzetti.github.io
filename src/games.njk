---
layout: page.njk
section: games
metaLabel: side projects · jams
title: Games
deck: Small things shipped, jammed, or abandoned honorably.
---
<ul class="index">
  {% for game in games %}
  <li>
    <p class="meta">{{ game.meta }}</p>
    <h2><a href="{{ game.url }}">{{ game.title }}</a></h2>
    <p>{{ game.blurb }}</p>
  </li>
  {% endfor %}
</ul>
